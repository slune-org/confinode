import { LoaderDescription, RequiringLoader } from '../Loader'

/**
 * Loader description.
 */
const description: LoaderDescription = {
  filetypes: 'wisp',
  Loader: RequiringLoader,
  module: 'wisp/engine/node',
}
export default description
